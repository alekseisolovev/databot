services:
  app:
    build: .
    command: jupyter lab --ip=0.0.0.0 --port=8888
    ports:
      - "8888:8888" 
    volumes:
      - .:/app 
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    env_file:
      - .env

